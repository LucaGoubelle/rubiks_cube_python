x, y, size

half = size / 2

for i...
    for j...
        draw(
            img1,
            x+(j*size)+(j*offset)-((i*half)+(i*offset)),
            y+(i*half)+(i*offset)
            face[i][j]
        );
